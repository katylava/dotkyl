*/10 * * * * fetchwork > /dev/null 2>&1
59 11 * * 1-4 say "Don't forget about standup"
# PIP_INDEX_URL override: gcloud's post-update `components update-macos-python`
# step pip-installs binary modules, and ~/.config/pip/pip.conf points pip at the
# work Artifact Registry, which 401s under cron and dumps a traceback.
05 12 * * * env PIP_INDEX_URL=https://pypi.org/simple/ gcloud components update --quiet 2>&1 | grep -v "All components are up to date" | grep -v '^$'

# vim: ft=crontab
