# Dotfiles Improvement Plans

## When asked "what can we work on?"

Only suggest items from the **Active candidates** section below. Everything
under **User-driven only** is off-limits as a suggestion — the user will
bring those up specifically when they want to tackle them. Do not list,
recommend, or hint at them in response to open-ended "what's next" /
"what else" prompts.

## Active candidates

_(none right now — propose new ideas as they come up, or surface
existing repo issues the user hasn't called out)_

## User-driven only — do not suggest

These are all tied to clean installs / new machines. The user tackles
them when a clean install is on the horizon, not as routine improvements.

- **`plan-audit-dotfiles.md`** — audit blocks on the work machine; the
  personal-host pass is done and surfaced nothing new to manage.
- **`plan-add-dotfile.md`** — blocked on the audit; possibly "not
  currently needed" depending on work-host findings.
- **`plan-new-machine-setup.md`** — `pre-new-install`, `post-new-install`,
  `apply-system-settings` mise tasks + `pre-new-install` Claude skill.
- **`plan-web-apps-tracker.md`** — sync mechanism for apps installed by
  web download.
- **`.ssh/` host-conditional routing** — only matters for clean installs.
- **`notes.md`** — human-managed file; do not edit or propose work on
  anything that lands there.

## Archived

Completed and abandoned plans have been removed from `.claude/`. See
`archived-plans.md` for the index (with the git commit to recover each).
